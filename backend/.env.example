# MongoDB Atlas Connection (DO NOT COMMIT REAL CREDENTIALS!)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/aile_hekimligi

# JWT Secret Key (Use strong random string, min 32 chars)
JWT_SECRET=change_this_to_random_string_minimum_32_characters

# Server Configuration
PORT=5000
NODE_ENV=production

# Frontend URLs (for CORS)
FRONTEND_URL=https://aile-hekimligi-kura.vercel.app

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
RATE_LIMIT_MAX=100           # Max 100 requests per window

# SMS Service (For TC verification - Optional)
SMS_ENABLED=false
SMS_API_KEY=your_sms_api_key
SMS_API_SECRET=your_sms_api_secret

# Security
SESSION_TIMEOUT=3600000      # 1 hour in milliseconds
MAX_LOGIN_ATTEMPTS=5         # Lock account after 5 failed attempts
LOCKOUT_DURATION=1800000     # 30 minutes lockout